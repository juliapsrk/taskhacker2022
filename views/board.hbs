<h2>{{board.name}}</h2>


<p>{{task.description}}</p>
<small>{{task.createdAt}}</small>

<br />

<a href="/team/{{team.creator._id}}">{{team.creator.name}}</a>
<a href="/profile/{{board.creator._id}}">{{board.creator.name}}</a>

<a href="/task/{{task._id}}/edit">Edit Task</a>

<form method="POST" action="/task/{{task._id}}/delete">
  <button>Delete Task</button>
</form>

{{!-- <div class="row">
  <div class="col-md-6">
    <form action="/movies/create" method="post" enctype="multipart/form-data"> {{!-- !!! --}}
      <div class="form-group">
        <label>Title:
          <input type="text" name="title" class="form-control" />
        </label>
      </div>
 
      <div class="form-group">
        <label>Description:
          <textarea name="description" cols="30" rows="4" class="form-control"> </textarea>
        </label>
      </div>
 
      <div class="form-group">
        <label>Attachments:
          <input type="file" name="attachment" class="form-control-file" />
        </label>
      </div>
 
      <button type="submit" class="btn btn-primary">Create</button>
    </form>
  </div>
</div> --}}